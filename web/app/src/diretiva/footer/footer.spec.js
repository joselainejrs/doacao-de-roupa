'use strict';

describe('myApp.src.diretiva.footer module', function() {

  beforeEach(module('myApp.src.diretiva.footer'));

  describe('footer controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var footerController = $controller('FooterController');
      expect(footerController).toBeDefined();
    }));

  });
});