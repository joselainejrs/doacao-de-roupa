'use strict';

describe('myApp.src.diretiva.bar module', function() {

  beforeEach(module('myApp.src.diretiva.bar'));

  describe('bar controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var menuController = $controller('MenuController');
      expect(menuController).toBeDefined();
    }));

  });
});