'use strict';

describe('myApp.diretiva.bar module', function() {

  beforeEach(module('myApp.diretiva.bar'));

  describe('bar controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var barController = $controller('BarController');
      expect(barController).toBeDefined();
    }));

  });
});